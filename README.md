# TickWars | A Text-Based Browser MMORPG Game

A Game where you influence yours, others and the global tick to your own strategical advantage.

## DEVELOPMENT BRANCH

Make sure to set up the relevant ENV Vars for your local environment.

### Recent Changes

#### 2025-05-20

- Configured a .env file and a handler to add the variables to the session
- Set up the projects Config file and have it load in main.go. The config is currently only has logic to read ENV variables currently as this is all i need for now.
- I have setup custom logging with multiple log files to keep them context-rich and separate concerns, making it easier to find something specific. I have also created a handler for each one, and provided subtle tweaks to each handler to reformat the datetime used in the logs. this now uses a more readable Y-M-D H:i:s
- Git is obviously all set up and i am building on the development branch
- learnt a fuck ton by deep diving into how things specifically work and why.

### Next Challenges

- Go through the project so far and set up scope privacy for structs and methods. Lowercase for private or where they should only be used within their own package, Using capitals for those i wish to be public and use within the project.
- Create SQLite DB and automigrate with Gorm to create the initial tables.
- Schemas will be generated by Gorm based on the structs i have set out.This way ensures the tables are effectively type-safe in context of the rest of the project, before we start entering crappy data in them.
- Work out how to create a seeder, so that Admin/Staff can be seeded into the tables before any players.
- Now i understand how middleware works in the 2 phases, i need to create my middleware groups and policies, then create tests and test them.
